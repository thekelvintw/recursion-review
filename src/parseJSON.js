// this is what you would do if you were one to do things the easy way:
// var parseJSON = JSON.parse;

// but you're not, so you'll write it from scratch:

// Strategy:
// declare current index curr
// declare next = curr+1
// Loop through the strings
// check first character (CH1) of input 
  // if CH1 === [
  // declare output = []
        // if CH2 === ], return []
        // if typeof next character = number 
       //ouput.push(next character) 
      // 




var parseJSON = function(json) {
  // your code goes here
};
